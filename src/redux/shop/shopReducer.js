const INITIAL_STATE={

}

const shopReducer = (state = INITIAL_STATE, action) => {
    switch (action.type) {
        case 'ACTION_TYPE':
            return 
        default:
            return state
    }
}
export default shopReducer;